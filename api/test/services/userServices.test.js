const { userService } = require('../../services');
const { User, VehicleInstances } = require('../../models');
var assert = require('assert');

describe("Feed User to web", () => {
    it("Feed user without vehicle config", () => { 
        const user = {
            _id: "5e8f8f7b9d8b2e0f0c8e8c9e",
            name: "Test",
            email: ""
            }
        
    });      
});